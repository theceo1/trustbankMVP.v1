import { Request, Response } from 'express';
import expressAsyncHandler from 'express-async-handler';

export const getWalletBalance = expressAsyncHandler(async (req: Request, res: Response) => {
  // Implementation
});
